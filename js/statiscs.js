let statistics = []

//This function
const generateStatistics = (data_matrix,column_vector,agrupate) => {
    statistics = []
    // for(let i=0; i<data_matrix.column_vector; i++){

    // }
    console.log(data_matrix);
    console.log(column_vector);
}

// nome[""]